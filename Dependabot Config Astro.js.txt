version: 2
updates:
  # Enable version updates for npm
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "04:00"
      timezone: "UTC"
    open-pull-requests-limit: 10
    reviewers:
      - "your-github-username" # Replace with your GitHub username
    assignees:
      - "your-github-username" # Replace with your GitHub username
    commit-message:
      prefix: "deps"
      prefix-development: "deps-dev"
      include: "scope"
    labels:
      - "dependencies"
      - "automated"
    # Group updates for better management
    groups:
      astro:
        patterns:
          - "astro*"
          - "@astrojs/*"
      react:
        patterns:
          - "react*"
          - "@types/react*"
      tailwind:
        patterns:
          - "tailwindcss*"
          - "@tailwindcss/*"
      dev-dependencies:
        dependency-type: "development"
        patterns:
          - "*"
    # Ignore specific packages if needed
    ignore:
      - dependency-name: "node-types"
        versions: ["< 18"]
    # Auto-merge minor and patch updates for non-security updates
    # Note: This requires GitHub branch protection rules to be configured
    target-branch: "main"
    
  # Enable version updates for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "04:00"
      timezone: "UTC"
    commit-message:
      prefix: "ci"
      include: "scope"
    labels:
      - "github-actions"
      - "automated"